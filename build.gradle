buildscript {
    ext {
        compose_ui_version = '1.2.0'
    }
}// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id 'com.android.application' version '7.4.0' apply false // Or a newer version
    id 'com.android.library' version '7.4.0' apply false // Or a newer version
    id 'org.jetbrains.kotlin.android' version '1.8.20' apply false // Example: Update to a newer Kotlin version
}